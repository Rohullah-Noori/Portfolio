
import Card from "./Card";

function Projects({ date, degree, university, description }) {
  return (
    <div>
      <Card data={data} degree={degree} university={university} description={} />
    </div>
  );
}

export default Projects;
